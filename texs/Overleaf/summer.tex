% ЛЕТНИЙ ОТЧЁТ (черновик). Стиль выдержан под зимний main.tex: простыми словами, не перегружено.
% Рисунки кладутся в подпапку figures/ (копируются из analysis/metrics/figures/).
% Все результаты заполнены на чистом протоколе (объединение фолдов {2} и {5}, исправленный матчер).
\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[russian]{babel}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{url}
\usepackage{geometry}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\geometry{a4paper, margin=2cm}

\lstset{
  keepspaces=true,
  extendedchars=true,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue!60!black},
  commentstyle=\color{gray},
  stringstyle=\color{green!50!black},
  numbers=left, numberstyle=\tiny\color{gray}, numbersep=6pt,
  breaklines=true, frame=single, language=Python, showstringspaces=false,
  morecomment=[l]{\#}
}

% мягкая заглушка для ещё не готовых рисунков
\newcommand{\MaybeImage}[2]{%
  \IfFileExists{#1}{\begin{center}\includegraphics[width=#2\linewidth]{#1}\end{center}}%
  {\begin{center}\fbox{\parbox[c][2.2cm][c]{0.8\linewidth}{\centering\small Рисунок в работе:\\\texttt{\detokenize{#1}}}}\end{center}}%
}

\title{Прогнозирование расщепления \\белков на пептиды}
\author{Шабаров Игорь}
\date{Июнь 2026}

\begin{document}
\maketitle
\tableofcontents

\newpage
\section{Введение}
\textbf{Белки} -- это состоящие из аминокислотных остатков биополимеры, они выполняют ключевые структурные, каталитические и регуляторные функции в клетках. Их функциональная активность определяется не только аминокислотной последовательностью, но и физико-химическими факторами, пространственной организацией, стабильностью и временем жизни в клетке.

Неправильно свернутые, повреждённые, короткоживущие регуляторные белки, а также чужеродные белки, попадающие в организм, подвергаются \textbf{протеолизу} -- ферментативному расщеплению белковых цепей специализированными протеазами. В ходе протеолиза образуются \textbf{пептиды} -- относительно короткие фрагменты белков, как правило длиной до нескольких десятков аминокислотных остатков. Эти пептиды могут обладать самостоятельной биологической активностью, участвовать в сигнальных каскадах, иммунных процессах и регуляции клеточной активности.

Понимание механизмов протеолиза и состава образующихся пептидов имеет особое значение в контексте разработки лекарственных препаратов и вакцин, многие из них несут в себе чужеродные белки, которые расщепляются на пептиды, распознаваемые иммунной системой. Таким образом, предсказание продуктов протеолиза позволяет более точно моделировать иммунный ответ и повышать эффективность биомедицинских разработок.

\subsection{Задача предсказания протеолиза и её значимость}
Поскольку в геномных и протеомных данных, как правило, представлена аминокислотная последовательность белка-предшественника, а не конечные продукты его расщепления, задача предсказания результатов протеолиза и границ пептидов является принципиально важной. Корректное предсказание этих процессов позволяет реконструировать набор потенциально образующихся пептидов и тем самым уточнять функциональную интерпретацию протеомных данных.

\subsection{Области применения}
\begin{itemize}
  \item \textbf{Протеомика и реконструкция протеолитических путей.}\\
  Модели предсказания протеолиза способствуют интерпретации данных масс-спектрометрии, позволяют сопоставить экспериментально наблюдаемые пептиды с белками-предшественниками, локализовать предполагаемые участки разреза и оценить влияние вариаций последовательности на паттерны расщепления.

  \item \textbf{Разработка терапевтических пептидов и вакцин.}\\
  На этапе дизайна позволяют быстро оценить «обрабатываемость» конструкций: какие фрагменты вероятно формируются \textit{in vivo}, а какие паттерны протеолиза могут приводить к нежелательным продуктам или ускоренной деградации. Это ускоряет поиск перспективных последовательностей и упрощает выявление неудачных кандидатов, тем самым повышая эффективность селекции препаратов.

  \item \textbf{Инженерия и дизайн белков.}\\
  Предсказание протеолиза используется для предотвращения нежелательного расщепления, целенаправленного использования управляющих паттернов и проектирования предшественников, образующих при расщеплении заданный набор пептидов в клеточных условиях. Тем самым модели выступают как автоматизированный фильтр качества при проектировании последовательностей.

  \item \textbf{Патологии протеолиза.}\\
  В ряде патологий (нейродегенеративных, онкологических, эндокринных) нарушается процесс протеолиза, появляются нежелательные паттерны разреза. Модели предсказания деградации белков помогают в их обнаружении при сопоставлении ожидаемых и наблюдаемых паттернов между контрольными и патологическими образцами, что позволяет формировать гипотезы о механизмах накопления токсичных фрагментов.
\end{itemize}

\section{Постановка задачи}
Дан белок-предшественник в виде последовательности аминокислот:
$$ x = (a_1, a_2, \dots, a_L), \qquad a_t \in \Omega, $$
где $\Omega$ -- алфавит из 20 аминокислот, а $L$ -- длина. Требуется предсказать, какие непрерывные участки этой последовательности станут после разрезания зрелыми \textbf{пептидами}, а какие -- \textbf{пропептидами}. Пропептид -- это вспомогательный участок, который отрезается и удаляется при созревании белка, но сам не является конечным активным продуктом.

Удобно сформулировать это как разметку каждой позиции последовательности одной из трёх меток
$$ y_t \in \{\textsf{None},\ \textsf{Peptide},\ \textsf{Propeptide}\}, $$
где \textsf{None} -- это участок, не входящий ни в зрелый пептид, ни в пропептид. Непрерывные участки одинаковой метки задают \textbf{сегменты} -- пары (начало, конец) с указанием типа (пептид или пропептид). То есть ответ модели -- это набор типизированных отрезков на последовательности. Схематичный пример (под каждым фрагментом подписан его тип):
$$
\underbrace{\texttt{MGK}}_{\textsf{None}}\;
\underbrace{\texttt{RALR}}_{\textsf{пропептид}}\;
\underbrace{\texttt{RSGK}}_{\textsf{пептид}}\;
\underbrace{\texttt{VR}}_{\textsf{None}}\;
\underbrace{\texttt{NL}}_{\textsf{пептид}}
$$

Отдельно стоит зафиксировать, что считается \textbf{правильным предсказанием}. Сопоставление предсказанных и настоящих сегментов выполняется \emph{раздельно для пептидов и для пропептидов} -- пептид сравнивается только с пептидами, пропептид только с пропептидами. Предсказанный сегмент засчитывается как верный (true positive), когда и его начало, и его конец совпадают с началом и концом соответственно настоящего сегмента того же типа с точностью до небольшого допуска $\pm\tau$ (используется $\tau=3$ остатка). Предсказанный сегмент, для которого такой пары не нашлось, считается ложным срабатыванием (false positive), а настоящий сегмент без пары -- пропуском (false negative). По этим подсчётам для каждого типа отдельно вычисляются точность (precision, доля верных среди предсказанных), полнота (recall, доля найденных среди настоящих) и их среднее гармоническое (F1). Допуск $\pm\tau$ отражает то, что биологически важно угадать приблизительный участок разреза, а не воспроизвести каждую позицию идеально.

\section{Обзор существующих решений}
Для этой задачи можно выделить два класса методов -- специализированные модели предсказания протеолитических пептидов и общие \textbf{белковые языковые модели} (protein language models, pLM), которые дают признаки последовательности.

\subsection{Специализированные модели, связанные с пептидным расщеплением}
\subsubsection{Модели с такой же постановкой задачи}
Одна из немногих моделей, прямо нацеленных на поставленную задачу -- \textbf{DeepPeptide}~\cite{deeppeptide2023}. При её обучении использовались аннотации типов \texttt{PEPTIDE} и \texttt{PROPEP} из \textbf{UniProtKB/Swiss-Prot}~\cite{uniprot2025}, она решает задачу как сегментацию предшественника. Её устройство включает:
\begin{itemize}
  \item По-остаточные эмбеддинги из языковой модели ESM-2~\cite{esm2_2019}
  \item Стек CNN--BiLSTM--CNN поверх эмбеддингов
  \item Линейно-цепной CRF с расширенным набором состояний, который собирает целостные сегменты с ограничением на длину
\end{itemize}
Такой декодер оптимизирует качество на уровне целых пептидов, а не отдельных позиций. В данной работе DeepPeptide берётся как сильная отправная точка, которую далее предлагается улучшить.

Есть также более старая модель с такой же постановкой задачи - \textbf{PeptideLocator}~\cite{mooney2013peptidelocator}, она рассматривалась как основной конкурент в статье DeepPeptide~\cite{deeppeptide2023}, и имела существенно менее значимые результаты. Кроме того, PeptideLocator вместо конкретных границ пептидов даёт только «тепловую карту» -- насколько каждый остаток похож на часть биоактивного пептида. 

\subsubsection{Модели с привязкой к протеазе}
Существует также ряд решений, предсказывающих результат при расщеплении белка строго ограниченным набором режущих протеаз, среди них:
\begin{itemize}
  \item \textbf{ProP}~\cite{duckert2004prop} - ищет только разрезы от семейства ферментов \texttt{PACE/PC}, и только у животных/растений-эукариот.
  \item \textbf{PROSPER}~\cite{song2012prosper}/\textbf{PROSPERous}~\cite{song2018prosperous} - предсказывает разрезы одного выбранного фермента за раз.
  \item \textbf{ProsperousPlus}~\cite{li2023prosperousplus} - конструктор, из выбранного фермента и предоставленных данных можно собрать модель для предсказания.
  \item \textbf{DeepCleave}~\cite{li2020deepcleave} - предсказывает разрезы только для каспаз и матриксных металлопротеиназ.
  \item \textbf{Procleave}~\cite{li2020procleave} - предсказывает разрезы от одного указанного фермента за раз, а также требует данные о 3D структуре белка.
\end{itemize}

\subsubsection{Модели с привязкой к организму или типу пептида}
Также есть класс моделей, специфичных для предсказания расщепления только нейропептидов, основные из них:
\begin{itemize}
  \item \textbf{NeuroPred}~\cite{southey2006neuropred} - видит только разрезы рядом с несколькими основными аминокислотами, а модель нужно выбирать отдельно под нужную группу животных. 
  \item \textbf{DeepNeuropePred}~\cite{wang2024deepneuropepred} - универсальна по видам, но также видит разрезы только по основным остаткам. 
  \item \textbf{NeuroPred-PLM}~\cite{wang2023neuropredplm} - не ищет места разрезов, а только предсказывает для готовой последовательности, похожа ли она на нейропептид.
\end{itemize}

\subsubsection{Модели для поиска другого вида пептидов}
Некоторые модели ищут не биологически активные пептиды, а сигнальные подпоследовательности, например \textbf{SignalP}~\cite{teufel2022signalp6} и \textbf{TargetP}~\cite{almagro2019targetp} находят и определяют тип сигнального пептида, который указывает куда белок будет транспортирован далее. 

\subsubsection{Модели для других пептидных задач}
При решении иных задач, связанных с пептидами, используются другие модели, такие как: \begin{itemize}
  \item \textbf{PeptideRanker}~\cite{mooney2012peptideranker}, \textbf{DeepBP}~\cite{zhang2024deepbp} - оценивают биоактивность готовой пептидной последовательности, расщепление не предсказывают.
  \item \textbf{BPFun}~\cite{bpfun2025} - предсказывает, какие из 7 функций свойственны данному пептиду. (противораковый, антибактериальный и т.д.)
  \item \textbf{pLM4ACE}~\cite{du2024plm4ace} - для готового пептида отвечает на вопрос - является ли он ингибитором АПФ, иными словами помогает ли он снижать артериальное давление.
\end{itemize}

\subsection{Языковые модели как источник эмбеддингов}
\begin{itemize}
  \item \textbf{ESM-2}~\cite{esm2_2019} -- трансформер-энкодер, обученный предсказывать замаскированные остатки, даёт контекстные по-остаточные эмбеддинги.
  \item \textbf{ESM Cambrian (ESM C)}~\cite{esm_cambrian_blog_2024} -- более новое семейство энкодеров, оптимизированное по качеству и ресурсам, рассматривается как замена ESM-2. Старшая версия содержит около 6 миллиардов параметров.
  \item \textbf{Ankh}~\cite{ankh2023} -- компактная encoder--decoder модель, на практике используется только её энкодер.
\end{itemize}
В этой задаче pLM применяются как «замороженные» энкодеры. Последовательность переводится в матрицу эмбеддингов $L \times d$, поверх которой обучается модель сегментации.

\subsection{Структурные и генеративные модели}
Структурные модели, в частности \textbf{AlphaFold~3}~\cite{alphafold3_2024}, восстанавливают трёхмерную структуру и дают пространственный контекст, недоступный из одной последовательности. Генеративные модели (\textbf{DiMA}~\cite{dima2024}) ориентированы на дизайн новых пептидов и могут служить на этапе генерации кандидатов. В данной работе структурный сигнал используется косвенно -- через предсказанный «структурный алфавит» 3Di (см.\ далее).

\subsection{Гомологичное разбиение и оценка}
\textbf{Гомологичными} называют последовательности общего эволюционного происхождения -- у них, как правило, похожи и аминокислотные цепочки, и функция. Если гомологичные белки окажутся одновременно и в обучении, и в тесте, модель сможет «узнать» тестовый белок по его близкому родственнику из обучения, тогда оценка качества будет завышена и не покажет, насколько хорошо модель работает на действительно новых последовательностях. Поэтому данные разбивают на фолды так, чтобы между фолдами было минимальное гомологичное сходство.

\textbf{Степень похожести} измеряют попарной \emph{идентичностью} последовательностей: две цепочки выравнивают друг относительно друга (глобальным выравниванием по Нидлмену--Вуншу) и считают долю позиций, где аминокислоты совпали. Для разбиения используется инструмент \textbf{GraphPart}~\cite{graphpart2023}: он раскладывает белки по фолдам так, чтобы попарная идентичность между последовательностями из \emph{разных} фолдов не превышала заданного порога (используется порог 30\%). Иными словами, любой тестовый белок отличается от всех обучающих минимум на 70\% позиций.

Оригинальный DeepPeptide оценивался \textbf{вложенной перекрёстной проверкой} (nested cross-validation) на 5 фолдах по следующей схеме: каждый из фолдов по очереди становится тестовым, а на оставшихся четырёх обучается модель, причём один из этих четырёх служит валидацией (для ранней остановки и подбора гиперпараметров). Так получается $5 \times 4 = 20$ обученных моделей, а итоговое качество усредняется по тестовым фолдам. Для применения 20 моделей объединяют в ансамбль. Подробнее преимущества и недостатки этой схемы разбираются в разделе~\ref{sec:protocol}.

\section{Базовая модель и её недочёты}
DeepPeptide -- разумная архитектура, но она не лишена недостатков. В ходе работы было обнаружено несколько моментов, которые важно учесть.

\subsection{Ошибка в подсчёте метрики разреза}
В исходном коде подсчёта метрик была найдена ошибка из-за переиспользования имён переменных во вложенном цикле. Функция \texttt{get\_counts\_for\_protein} (файл \texttt{src/utils/manuscript\_metrics.py}) перебирает истинные пептиды во внешнем цикле и предсказанные во внутреннем, используя дважды одни и те же имена \texttt{idx} и \texttt{row}:

\begin{lstlisting}
for idx, row in true_df.iterrows():          # idx -- true peptide index
    true_start, true_stop = row['start'], row['stop']
    for idx, row in pred_df.iterrows():       # idx rewrited with prediction index
        pred_start, pred_stop = row['start'], row['stop']
        ...
        if start_match and stop_match:
            true_df.loc[idx, 'matched'] = True  # BUG: idx -- prediction index, not true
            pred_df.loc[idx, 'matched'] = True
            break
\end{lstlisting}

После входа во внутренний цикл переменная \texttt{idx} указывает уже на предсказанный пептид, поэтому строка \texttt{true\_df.loc[idx,\,'matched'] = True} помечает не тот истинный пептид, для которого нашлось совпадение. В результате часть найденных совпадений не засчитывается правильной группе истинных пептидов, и полнота (recall), а вместе с ней и F1, \textbf{занижаются}. Эта функция вызывается из \texttt{compute\_peptide\_finding\_metrics} и \texttt{compute\_all\_metrics}, то есть участвует в подсчёте именно тех $\pm3$-метрик уровня пептидов, которые приводятся в оригинальной работе.

После исправления (раздельные имена переменных во внутреннем цикле) числа поднимаются примерно на $0.01$--$0.015$ почти равномерно по всем моделям, и порядок моделей не меняется, можно сказать что ошибка не искажала относительные выводы. Все приводимые далее числа получены исправленным подсчётом.

\subsection{Как выбиралась архитектура}
Вложенная перекрёстная проверка честно настраивает гиперпараметры, не заглядывая в тестовый фолд. Однако сам \emph{выбор архитектуры} (какие блоки добавить, какую языковую модель взять) в оригинале делался на тех же фолдах, на которых затем приводятся результаты. Если человек перебирает варианты архитектуры и оставляет тот, что лучше показал себя на этих же фолдах, оценка для лучшего варианта оказывается смещённой вверх -- ровно та проблема, от которой защищает отдельный отложенный набор для тестирования. Кроме того, итоговая модель для применения (ансамбль из 20 сетей) на честно отложенных данных вообще не измерялась. Поэтому опубликованное число, вероятно, несколько оптимистично. Это наблюдение и подтолкнуло к построению более аккуратного протокола оценки (раздел~\ref{sec:protocol}).

\section{Данные: от 2022 к 2026}
Исходный датасет DeepPeptide собран из аннотаций \texttt{PEPTIDE} и \texttt{PROPEP} базы Swiss-Prot (версия 2022 года). Первое предлагаемое улучшение -- расширить данные: датасет был пересобран по исходной методике на свежей версии базы (2026 год). Белков стало заметно больше, и, что важнее, добавились последовательности из новых, не представленных ранее семейств. Сводка приведена ниже (таблица~\ref{tab:data}).
\begin{table}[htbp]\centering
\caption{Состав датасета: версия 2022 года против пересобранной версии 2026 года.}
\label{tab:data}
\small
\begin{tabular}{lrr}
\hline
 & \textbf{2022} & \textbf{2026} \\
\hline
Белков & 8449 & 9619 \\
\quad из них новых (не было в 2022) & --- & 1178 ($\approx$12\,\%) \\
Пептидных подпоследовательностей & 6372 & 7431 \\
Пропептидных подпоследовательностей & 8211 & 9140 \\
Длина пептидов (медиана, межквартильный размах) & 21 (11--31) & 20 (11--30) \\
\hline
\end{tabular}
\end{table}

Данные сильно неоднородны. По организмам в 2026 году представлено более тысячи родов: от человека, мыши и крысы до растений (\textit{Arabidopsis}) и ядовитых животных -- морских улиток-конусов (\textit{Conus}, самый частый род -- около 900 белков) и пауков (\textit{Cyriopagopus}, \textit{Lycosa}). Такая неоднородность -- важное обстоятельство: разные семейства устроены по-разному и предсказываются с разным качеством, и это напрямую влияет на сравнение моделей (раздел~\ref{sec:protocol}). Распределения по длинам, типам и организмам показаны на рис.~\ref{fig:data}.

\begin{figure}[htbp]\centering
\MaybeImage{figures/data_distributions.png}{0.9}
\caption{Состав данных 2026 года: распределение сегментов по длине, по типу (пептид/пропептид) и по родам организмов.}
\label{fig:data}
\end{figure}

\paragraph{Ограничение по длине сегментов (5--50 остатков).} В рассматриваемых данных учитываются только пептиды и пропептиды длиной от 5 до 50 остатков, важно подчеркнуть, что это не свойство самих аннотаций Swiss-Prot, а решение, заложенное в модель и предобработку: декодер DeepPeptide -- это CRF с конечным набором состояний, где каждый сегмент проходит состояния «начало $\to$ внутренние $\to$ конец», и в исходной реализации число состояний необходимо чётко задать. Выбор именно 5-50 обосновывается следующим образом: \textbf{верхняя граница 50} затрагивает только самый хвост распределения длин -- наиболее длинные сегменты: в датасете 2026 года под отсечение попадают лишь самые длинные пропептиды (примерно до 79 остатков), а основная масса аннотированных сегментов заметно короче этого порога (рис.~\ref{fig:data}). \textbf{Нижняя граница 5} обусловлена самой метрикой: при допуске $\pm3$ совпадение для сегмента короче 5 остатков охватывало бы практически весь сегмент, делая оценку границ бессмысленной, но на самом деле число 5 не так принципиально. После фильтрации все оставшиеся сегменты по построению лежат в диапазоне 5--50.

\section{Чистый протокол оценки}\label{sec:protocol}
Чтобы сравнивать модели честно и отделить выбор архитектуры от итоговой оценки, был построен новый протокол обучения и оценки на данных 2026 года.

\paragraph{Разбиение и роли фолдов.} Метод предобработки из статьи был воспроизведён, но с б\'{о}льшим количеством частей: данные 2026 года разбиты на 7 фолдов с помощью GraphPart (порог идентичности 30\%) и сбалансированы по флангующим мотивам областей разреза, затем за фолдами были явно закреплены \emph{роли}:
\begin{itemize}
  \item \textbf{train} (4 фолда) -- обучение
  \item \textbf{dev} (1 фолд) -- выбор лучшей эпохи (ранняя остановка)
  \item \textbf{model-select} (1 фолд) -- сравнение архитектур между собой
  \item \textbf{sealed} (1 фолд) -- изначально планировался как запечатанный финальный тест
\end{itemize}
Доля обучающих данных при этом почти не изменилась по сравнению с оригиналом: 4 из 7 фолдов ($\approx$57\,\%, около 5400 белков) против 3 из 5 фолдов (60\,\%) в каждой из 20 моделей вложенной проверки. Главная идея -- отделить выбор архитектуры от итоговой оценки, чего не хватало оригинальному методу.

\paragraph{Балансировка по флангующим мотивам.} Распределить белки по фолдам с учётом только гомологии недостаточно: места разреза бывают разной биохимической природы (разные протеазы узнают разные мотивы), и если один класс мотивов случайно соберётся в одном фолде, оценка будет смещённой. Чтобы этого избежать, фолды дополнительно балансируются по \emph{мотиву места разреза}, повторяя рецептуру оригинальной статьи. Для каждого сегмента берутся по два остатка \emph{снаружи} от его границ -- два перед началом и два после конца (\texttt{[N-2,\,N-1,\,C+1,\,C+2]}), это и есть локальный контекст, который «видит» протеаза. Каждый из четырёх остатков кодируется эмбеддингом ESM-2, четыре вектора склеиваются в один признак, и все места разреза кластеризуются методом $k$-средних ($k=50$). Так каждый разрез получает метку «класс мотива», а каждому белку присваивается его преобладающий класс. Эти метки передаются в GraphPart как балансируемый признак: разбиение ищется так, чтобы, помимо ограничения на гомологию между фолдами, классы мотивов были примерно одинаково представлены во всех фолдах.

\paragraph{Проблема: фолды неэквивалентны.} Балансировка делалась по мотивам разреза, но \emph{не} по длинам пептидов и не по организмам. В итоге фолды заметно различаются по составу, а результаты на разных фолдах сильно расходятся: модели «меняются местами» в рейтинге от фолда к фолду, а отдельные семейства (например, яд паука \textit{Cyriopagopus}) на отдельных фолдах проседают на десятки пунктов. Это эффект \emph{состава фолда}, а не самих моделей (рис.~\ref{fig:folds}).

\begin{figure}[htbp]\centering
\MaybeImage{figures/fold_divergence.png}{0.9}
\caption{Расхождение результатов между фолдами: один и тот же набор моделей по-разному ранжируется на разных фолдах, а отдельные семейства резко проседают на «своих» фолдах. Это эффект состава фолда, а не качества моделей.}
\label{fig:folds}
\end{figure}

\paragraph{Полное решение и компромисс.} Полностью эту проблему лечит полная перекрёстная проверка (nested cross-validation): тогда каждый белок ровно один раз попадает в тест, и редкие семейства взвешиваются по своей настоящей частоте. Но это многократно дороже по вычислениям из-за необходимости отдельных фолдов под эпоху и выбор модели, поэтому пока такая проверка не проводилась. Вместо неё \textbf{объединяются два отложенных фолда} (model-select и sealed), и модели сравниваются на их объединении. Удачно, что эти два фолда комплементарны по длинам (в одном больше коротких пептидов, в другом -- длинных), поэтому их отклонения компенсируют друг друга (новый L1 - 0.102), а размер выборки удваивается (около 2{,}3 тыс.\ белков). Платой становится то, что не остаётся отдельного тестового фолда, который вообще не использовался при выборе архитектуры. Но такого фолда не было и в оригинальном DeepPeptide, поэтому это помечается как ограничение. Далее в местах, где важно показать, где и какая модель сильна, используется оценка по подвыборкам (длина, тип, организм): доля верно найденных сегментов внутри подвыборки не зависит от состава фолда, поэтому объединение здесь корректно.

\section{Эксперименты и результаты}
Далее представлены результаты основных экспериментов, полный же набор показан в разделе~\ref{sec:allexp}.
Подробно модификации описаны далее в разделе~\ref{sec:modifications}.
Все числа ниже получены на объединённых отложенных фолдах с исправленной метрикой $\pm3$. Общее сравнение моделей показано на рис.~\ref{fig:scoreboard}, сводка по проверенным модификациям -- в таблице~\ref{tab:verdict}.

\paragraph{Доверительные интервалы.} На графиках приводятся 95\,\%-е доверительные интервалы, полученные \emph{бутстрапом по белкам}. Сама точечная оценка (например, F1) считается один раз на всём отложенном наборе. Чтобы оценить её разброс, из $N$ отложенных белков многократно ($B=2000$ раз) формируется новая выборка того же размера $N$ \emph{с возвращением} и на каждой такой выборке метрика пересчитывается заново. Доверительный интервал -- это 2{,}5-й и 97{,}5-й процентили полученного распределения из $B$ значений. Тем самым интервал отражает, насколько оценка зависит от того, какие именно белки попали в отложенный набор. Он не учитывает разброс от случайной инициализации и обучения, поэтому истинная неопределённость несколько шире, а приводимые интервалы -- её консервативная оценка. Для сравнения двух моделей (разностей метрик) бутстрап \emph{парный}: на каждой итерации обе модели оцениваются на одной и той же случайной выборке белков, что убирает общий для них разброс по белкам и оставляет именно разницу.

\begin{figure}[htbp]\centering
\MaybeImage{figures/scoreboard.png}{1.0}
\caption{Сравнение моделей на объединённых отложенных фолдах: F1, точность и полнота с 95\,\%-ми доверительными интервалами. Улучшение от ESM-2 до полной архитектуры на ESM-C 6B.}
\label{fig:scoreboard}
\end{figure}

\begin{table}[htbp]\centering
\caption{Сводка по проверенным модификациям: к какому типу относится изменение, его измеренный эффект на F1 (объединение фолдов \{2\} и \{5\}, относительно подходящего контроля) и итоговый вердикт.}
\label{tab:verdict}
\small
\begin{tabular}{p{4.7cm} p{2.9cm} p{4.7cm} p{2.6cm}}
\hline
\textbf{Модификация} & \textbf{Тип} & \textbf{Эффект на F1} & \textbf{Вердикт} \\
\hline
ESM-C 6B вместо ESM-2 & замена эмбеддинга & $+0.03$, неустойчиво по фолдам & слабо помогает \\
Boundary-голова на ESM-C 6B & архитектурная надстройка & ${+}0.05, +0.07$, уверенно & \textbf{помогает} \\
Boundary-голова на ESM-2 & архитектурная надстройка & $\approx 0$ (ДИ включает ноль) & без эффекта \\
Структурный канал 3Di & доп. эмбеддинг & пропеп. ${+}0.02$, пеп. ${-}0.04$, в сумме нейтрально & компромисс \\
Сжатие ESM-C 6B: 2560$\to$256 & оптимизация & $\approx 0$, модель по входу приблизительно в $16$ раз меньше & безвредно \\
Bond-лосс & доп.\ функция потерь & пеп. ${-}0.05$, в сумме ${-}0.015$ & скорее вредит \\
Телескопический сегментный CRF & архитектурная надстройка & $\approx 0$ & без эффекта \\
\hline
\end{tabular}
\end{table}

\subsection{Проверявшиеся модификации}\label{sec:modifications}
Прежде чем переходить к результатам, опишем сами проверявшиеся изменения. Все они надстраиваются над базовой схемой DeepPeptide (pLM $\to$ стек CNN--BiLSTM $\to$ CRF).

\paragraph{Замена эмбеддингов.} Самое простое изменение -- поменять «замороженную» языковую модель, которая выдаёт по-остаточные признаки. Сравнивались ESM-2 (1280 признаков на остаток), ESM-C 600M (1152) и ESM-C 6B (2560). Остальная часть модели при этом не меняется.

\paragraph{Boundary-голова (предсказывает границы разреза).} Это небольшой полносвязный блок (LayerNorm $\to$ линейный слой $\to$ GELU $\to$ линейный слой), который работает поверх контекстных по-остаточных признаков (выход CNN--BiLSTM). Для каждой позиции и каждого типа (пептид, пропептид) boundary-голова выдаёт три числа: насколько эта позиция похожа на \emph{начало} сегмента, на его \emph{внутренность} и на \emph{конец}. Эти числа добавляются как поправки к эмиссиям соответствующих состояний CRF -- состояния начала (№ 1), внутренних состояний(№ 2-49) и состояния конца(№ 50). Последний слой инициализируется нулями, поэтому в начале обучения модель в точности совпадает с базовой и учится «заострять» границы только если это снижает ошибку.

\paragraph{Bond-лосс.} Вспомогательный блок: для каждой связи между соседними остатками $i$ и $i{+}1$ по контекстным CNN--BiLSTM признакам $[h_i,\ h_{i+1},\ |h_{i+1}-h_i|]$ он предсказывает, проходит ли между этими соседними остатками разрез. Этот блок работает только как дополнительный обучающий сигнал (на этапе предсказания не используется) и должен подталкивать общие признаки лучше кодировать места разреза.

\paragraph{Структурный канал 3Di.} Помимо аминокислотной последовательности, для каждого остатка можно получить букву «структурного алфавита» 3Di (20 возможных букв, предсказываются моделью ProstT5) -- она грубо кодирует локальное пространственное окружение остатка. Этот 20-мерный признак конкатенируется к эмбеддингу языковой модели. Чтобы соединить два разнородных источника, используется gated-проектор: он по отдельности сжимает основной pLM эмбеддинг (в случае ESM-C 6B: 2560 $\to$ 256 признаков) и проектирует структурный канал (20 $\to$ 16), после чего объединяет их и подаёт в CNN--BiLSTM--CRF. Сжатие 2560$\to$256 и ширина структурной проекции (16/32/48) -- отдельные параметры, которые тоже проверялись.

\paragraph{Телескопический сегментный CRF.} Это способ заставить ту же CRF оценивать сегмент \emph{целиком и с учётом длины}, не меняя её динамическое программирование. В базовой схеме все состояния одного типа (например, все 50 «пептидных» состояний) получают \emph{одну и ту же} эмиссию: модель не различает, на каком месте внутри сегмента стоит остаток. Телескопическая версия устроена так: отдельная голова предсказывает для каждого остатка и каждого типа «предпочтительную относительную координату» $r\in[0,1]$ ($0$ -- остаток похож на начало сегмента, $1$ -- на конец). Для сегмента-кандидата $[s,e]$ фактическая относительная координата остатка $i$ равна $t=(i-s)/(e-s)$, и всему сегменту приписывается оценка, штрафующая рассогласование предсказанной и фактической координаты:
$$\mathrm{SpanScore}(s,e)=\sum_{i=s}^{e} -\,\frac{|\,t(i)-r_i\,|}{\tau},$$
Ключевой приём -- эмиссии состояний делаются \emph{телескопическими разностями}:
$$\Delta(i,k)=\mathrm{SpanScore}(i-k+1,\,i)-\mathrm{SpanScore}(i-k+1,\,i-1),$$
так что вдоль пути CRF по сегменту они в сумме дают ровно $\mathrm{SpanScore}(s,e)$ -- промежуточные слагаемые взаимно сокращаются (отсюда название). Тем самым обычный путь CRF начинает оценивать весь сегмент, награждая внутренне согласованную «разметку» координаты от начала к концу, и при этом не нужна отдельная сегментная реализация. На чистом протоколе этот вариант идёт вровень с базовой схемой состояний -- ощутимого эффекта нет.

\subsection{Данные: объём, покрытие и схожесть с обучающей выборкой}\label{sec:data}
Самый сильный практический рычаг качества -- это \textbf{данные}, но важно правильно понимать, \emph{что} именно помогает. Не объём сам по себе, а \textbf{покрытие}: насколько близко к каждой тестовой последовательности есть что-то в обучении. Ниже это разбирается на трёх взаимосвязанных наблюдениях.

\paragraph{Объём данных -- рычаг для слабой модели, но не для сильной.} Каждая из трёх ключевых моделей (базовая ESM-2, ESM-C 6B с boundary-головой без 3Di, она же с 3Di) обучалась на возрастающих долях обучающих фолдов -- 40, 55, 70, 85 и 100\,\%, -- а оценивалась всегда на одних и тех же объединённых фолдах (рис.~\ref{fig:datascale}). Картина оказалась показательно \emph{разной} для слабой и сильной модели:
\begin{itemize}
  \item \textbf{Слабая ESM-2 ограничена объёмом данных.} F1 растёт с $0.50$ при 40\,\% до $0.58$ при 85\,\% (примерно ${+}0.085$) -- этой модели данных вероятно не хватает.
  \item \textbf{Сильная ESM-C 6B к объёму почти нечувствительна.} Уже при 40\,\% данных ($\approx 2100$ белков) обе её версии дают $\approx 0.68$--$0.69$ и до 100\,\% практически не меняются (${+}0.02$).
\end{itemize}
Объяснение в том, что ESM-C 6B предобучена на гигантском белковом корпусе и уже несёт в себе локальный контекст вокруг мест разреза. Извлечь из такого эмбеддинга \emph{детекцию} разреза с допуском $\pm3$ -- задача по-видимому несложная, и для неё хватает небольшого числа примеров. Поэтому добавление новых данных \emph{той же} обучающей выборки почти не двигает $\pm3$-метрику сильной модели: её узкое место не в объёме.

\begin{figure}[htbp]\centering
\MaybeImage{figures/datascale_curve.png}{0.8}
\caption{F1 ($\pm3$) в зависимости от числа обучающих белков на объединённых фолдах. Слабая ESM-2 заметно растёт с объёмом данных, обе версии на ESM-C 6B почти не меняются -- они близки к насыщению уже при 40\,\% данных.}
\label{fig:datascale}
\end{figure}

\paragraph{Но точность границ растёт с данными даже у сильной модели.} Если перейти от детекции ($\pm3$) к точному попаданию в границы ($\pm0$), картина меняется: здесь данные помогают всем моделям, в том числе ESM-C 6B (рис.~\ref{fig:datatol}). У сильной модели F1 при $\pm3$ почти плоский ($\Delta\approx{+}0.02$ от 40 к 100\,\%), а F1 при точном совпадении $\pm0$ за тот же диапазон растёт на $\Delta\approx{+}0.05$ (с $0.46$ до $0.51$), доля сохранённого качества при ужесточении допуска поднимается с $0.67$ до $0.73$. То есть данные не добавляют новых \emph{обнаружений} сильной модели, но делают её разрезы \emph{точнее} -- модель чаще попадает в саму позицию разреза, а не просто в его окрестность.

\begin{figure}[htbp]\centering
\MaybeImage{figures/datascale_tolerance.png}{1.0}
\caption{Точность границ в зависимости от объёма данных. Слева -- ESM-C 6B: при допуске $\pm3$ кривая почти плоская, при $\pm0$ растёт. Справа -- доля сохранённого F1 при переходе к точному совпадению для трёх моделей. Числами подписаны приросты от 40 к 100\,\%.}
\label{fig:datatol}
\end{figure}

\paragraph{Как именно данные помогают.} Глубинная причина, по которой данные так важны, видна из разбора отложенных предсказаний (рис.~\ref{fig:similarity}). Полнота на конкретном сегменте \emph{монотонно} растёт с его максимальной идентичностью к обучающим сегментам: для пептидов она поднимается с $\approx 0.39$ при идентичности ниже 30\,\% до $\approx 0.85$ при идентичности выше 70\,\% (для всех сегментов вместе -- с $0.42$ до $0.80$). При этом \emph{представленность организма не оказывает влияния}: ранговая корреляция между тем, насколько часто род встречается в обучении, и полнотой на нём близка к нулю ($\rho \approx 0$). Иными словами, частый в обучении организм не предсказывается лучше -- лучше предсказывается последовательность, \emph{похожая} на что-то из обучения. А поскольку фолды разведены по гомологии, отложенные белки -- это как раз «непохожий хвост», самый трудный случай. Отсюда и роль данных: расширяя обучение, мы повышаем шанс, что рядом с очередной тестовой последовательностью найдётся похожая, и тем самым поднимаем полноту.

\begin{figure}[htbp]\centering
\MaybeImage{figures/similarity.png}{1.0}
\caption{Полнота на пептидах против (а) числа обучающих сегментов рода -- представленность ($\rho\approx 0$) и (б) максимальной похожести сегмента к обучающим -- похожесть (монотонный рост). Идентичность измеряется по отдельным сегментам, а не по целым белкам.}
\label{fig:similarity}
\end{figure}

\noindent\emph{Вывод по данным.} Рычаг качества -- это покрытие обучающими данными пространства последовательностей. Для слабого эмбеддинга больше данных напрямую поднимают детекцию, а для сильного эмбеддинга детекция уже почти насыщена предобучением, но данные продолжают \emph{заострять} границы разреза. И в обоих случаях работает один механизм: предсказывается то, что похоже на обучение.

\subsection{Архитектура: boundary-голова работает только на богатых эмбеддингах}
Boundary-голова (описана выше) отдельно учится предсказывать места разреза. Следующие три наблюдения вместе дают аккуратный вывод.
\begin{itemize}
  \item \textbf{Голова на богатых эмбеддингах (ESM-C 6B)} даёт устойчивый прирост $+0.05$--$0.07$ F1 на обоих фолдах.
  \item \textbf{Богатые эмбеддинги сами по себе} почти не помогают: «голый» ESM-C 6B обгоняет ESM-2 лишь примерно на $0.03$, и этот выигрыш неустойчив (на разных фолдах меняет знак).
  \item \textbf{Boundary-голова на слабых эмбеддингах (ESM-2)} не даёт почти ничего: прирост $+0.021$ и $+0.005$ на двух фолдах, причём доверительный интервал включает ноль. Для сравнения, на ESM-C 6B та же добавка даёт $+0.053$ и $+0.068$ с интервалом уверенно выше нуля.
\end{itemize}
Отсюда вывод: устойчивый прирост даёт именно \emph{взаимодействие} «boundary-голова $\times$ богатые эмбеддинги», а не каждый из компонентов по отдельности (рис.~\ref{fig:interaction}). Это объясняет, почему модификации заметно помогли на ESM-C, но почти ничего не дали на ESM-2: boundary-голова умеет извлекать сигнал о границах только тогда, когда он уже присутствует в богатых эмбеддингах.

\begin{figure}[htbp]\centering
\MaybeImage{figures/interaction.png}{0.9}
\caption{Взаимодействие «boundary-голова $\times$ эмбеддинг»: прирост F1 от добавления boundary-головы поверх ESM-2 и поверх ESM-C 6B, с 95\,\%-ми доверительными интервалами. На слабом эмбеддинге интервал включает ноль, на богатом -- уверенно выше нуля.}
\label{fig:interaction}
\end{figure}

\paragraph{За счёт чего и где помогает boundary-голова.} Она делает границы сегментов более чёткими. Это видно, если постепенно ужесточать допуск совпадения с $\pm3$ до точного $\pm0$ (рис.~\ref{fig:tolerance}): модели с boundary-головой теряют качество заметно медленнее. Доля сохранённого F1 при переходе к точному совпадению (отношение F1 при $\pm0$ к F1 при $\pm3$) растёт: $0.55$ у базовой ESM-2, $0.65$ при замене pLM на ESM-C 6B, $0.68$ после добавления boundary-головы и $0.72$--$0.73$ с проектором и 3Di. В абсолютных числах F1 при точном совпадении поднимается с $0.32$ (база) до $0.45$ (boundary на 6B) и $0.50$--$0.51$ (полная модель). Где именно сосредоточен прирост, видно из разбора качества по длинам сегментов (рис.~\ref{fig:bylength}): сильная модель лучше \emph{находит} длинные пептиды (растёт полнота) и реже ошибается на коротких предсказаниях (растёт точность).

\begin{figure}[htbp]\centering
\MaybeImage{figures/tolerance.png}{1.0}
\caption{Качество при ужесточении допуска совпадения границ с $\pm3$ до точного ($\pm0$), на объединённых фолдах. Слева -- абсолютный F1, справа -- доля сохранённого F1. Чем медленнее падает кривая, тем точнее модель угадывает сами границы разреза.}
\label{fig:tolerance}
\end{figure}

\begin{figure}[htbp]\centering
\MaybeImage{figures/bylength.png}{1.0}
\caption{F1, точность и полнота в зависимости от длины сегмента, на объединённых фолдах. Прирост сильной модели сосредоточен на полноте длинных пептидов и на точности коротких предсказаний.}
\label{fig:bylength}
\end{figure}

\subsection{Дополнительные каналы и оптимизации}
Все сравнения ниже -- парные, на объединённых фолдах, относительно подходящего контроля (в квадратных скобках -- 95\,\%-й доверительный интервал разности).
\begin{itemize}
  \item \textbf{Структурный канал 3Di} даёт \emph{компромисс}, а не общий прирост. Если сравнить полную модель с её же версией, где 3Di-канал занулён (та же архитектура), то на пропептидах F1 растёт на ${+}0.024$ $[{+}0.014;\,{+}0.035]$, а на пептидах падает на ${-}0.042$ $[{-}0.061;\,{-}0.024]$ -- в основном за счёт полноты (recall пептидов $0.66$ против $0.74$). В сумме по обоим типам -- ничья: $\Delta\text{F1}={-}0.002$ $[{-}0.012;\,{+}0.008]$, интервал включает ноль.
  \item \textbf{Сжатие эмбеддинга} с 2560 до 256 признаков практически не меняет качество: F1 $0.692$ против $0.694$ без сжатия, $\Delta={-}0.003$ $[{-}0.017;\,{+}0.013]$ (в пределах шума). Значит, модель можно сделать примерно в 16 раз компактнее по входу проектора без потери качества, что проверено экспериментально.
  \item \textbf{Bond-лосс} оказался скорее вредным. На богатом эмбеддинге (ESM-C 6B с boundary-головой) добавление bond снижает общий F1 на ${-}0.015$ $[{-}0.028;\,{-}0.002]$, причём весь урон -- на пептидах (${-}0.054$ $[{-}0.075;\,{-}0.032]$, recall $0.57$ против $0.64$) при крошечном плюсе на пропептидах (${+}0.015$). Это не равноценный компромисс, а близко к вредному добавлению.
\end{itemize}
Любопытно, что 3Di и bond ведут себя одинаково (рис.~\ref{fig:trades}) -- оба смещают модель в сторону пропептидов ценой полноты на пептидах, только у bond этот размен явно невыгоден.

\begin{figure}[htbp]\centering
\MaybeImage{figures/trades.png}{1.0}
\caption{Изменение точности (P) и полноты (R) по типам сегментов при добавлении 3Di и bond-лосса (объединение фолдов \{2\} и \{5\}). Оба канала повышают качество на пропептидах и снижают полноту на пептидах, у bond размен невыгоден и в сумме.}
\label{fig:trades}
\end{figure}

\subsection{Прочие проведённые эксперименты}\label{sec:allexp}
Помимо описанного выше, был проверен большой набор более ранних идей, по ним есть сводная таблица с исправленной метрикой, но все они оценивались по \emph{старому} методу -- на разбиении 2022 года и с выбором модели по валидационному фолду (том же, что использовался для ранней остановки), и без кросс-валидации, а с фиксированными ролями фолдов. Поэтому к их результатам нужно относиться осторожно и перепроверять на чистом протоколе. Ниже коротко описано, что именно проверялось.
\begin{itemize}
  \item \textbf{Словарь известных пептидов (Aho).} Идея -- подсказать модели уже известные пептиды. Собирался словарь всех пептидов из набора датасетов, и быстрым поиском подстрок (алгоритмом Ахо--Корасик, отсюда название) в новой последовательности отмечались вхождения известных пептидов. Этот сигнал подмешивался к модели разными способами: как добавка к эмиссиям CRF, как отдельный вход, как поправка к переходам CRF, как ранняя конкатенация с эмбеддингами. Заметного устойчивого выигрыша эти варианты не дали.
  \item \textbf{Структурные признаки.} Вместо предсказанного алфавита 3Di пробовались признаки, извлечённые из предсказанных трёхмерных структур, в частности с помощью AlphaFold (с разными вариантами фильтрации по достоверности структуры). Сюда же относится сама модель ProstT5 как источник структурного сигнала.
  \item \textbf{Дообучение языковой модели (LoRA).} Вместо полностью «замороженного» ESM-2 пробовалось лёгкое дообучение нескольких последних слоёв методом LoRA (низкоранговые поправки к матрицам внимания). Прироста это не принесло, при этом усложняет и удорожает обучение.
  \item \textbf{Варианты проекторов.} Мультимасштабные и многоветочные (tribranch) проекторы поверх эмбеддингов -- разные способы перекодировать и смешивать признаки перед CNN--BiLSTM.
  \item \textbf{Ширина структурной проекции и телескопический CRF.} Ширина проекции структурного канала 3Di (16/32/48 признаков) на качество не влияет, поэтому в основную таблицу она не вынесена, телескопический сегментный CRF (см.\ выше) идёт примерно вровень с базовой схемой и при старом методе оценки.
\end{itemize}
Из всего перечисленного ни одна идея не дала на старом методе устойчивого прироста, который оправдывал бы приоритетный перенос на чистый протокол.

\section{Выводы}
Работа была построена вокруг честного сравнения модификаций DeepPeptide на пересобранных данных 2026 года, главные итоги можно сгруппировать так.

\paragraph{Про данные.} Самый сильный рычаг качества -- это покрытие обучающими данными, при этом эффект данных \emph{различается} по моделям и по строгости задачи:
\begin{itemize}
  \item Слабая ESM-2 ограничена объёмом данных (F1 растёт на ${+}0.085$), а сильная ESM-C 6B к объёму почти нечувствительна -- богатый предобученный эмбеддинг во многом \emph{заменяет} обучающие данные на уровне детекции $\pm3$.
  \item  Даже у сильной модели данные продолжают \emph{заострять границы}: при ужесточении допуска до точного совпадения $\pm0$ качество растёт с объёмом данных (${+}0.05$ F1).
  \item Важна не представленность организма ($\rho\approx 0$), а \textbf{похожесть} тестовой последовательности к обучающим (полнота на пептидах растёт с $0.39$ до $0.85$ по мере роста идентичности), поэтому ценно широкое покрытие данных.
\end{itemize}

\paragraph{Про архитектуру.} Единственная \emph{архитектурная} надстройка с устойчивым выигрышем из проверенных -- это \textbf{boundary-голова поверх богатых эмбеддингов} (${+}0.05\dots{+}0.07$ F1). Это именно \emph{взаимодействие}: ни более сильный эмбеддинг сам по себе, ни boundary-голова на слабом ESM-2 устойчивого прироста не дают. Помогает голова за счёт того, что делает сами границы разреза точнее.

\paragraph{Про дополнительные каналы и оптимизации.} Структурный канал 3Di -- это компромисс (${+}0.02$ на пропептидах, ${-}0.04$ на пептидах, в сумме ничья), сжатие эмбеддинга $2560\to256$ безвредно и позволяет уменьшить модель примерно в 16 раз по входу проектора. Блоки, оставленные в основной модели (boundary-голова, 3Di, сжатие), оправданы численно: каждый из них либо помогает, либо несёт понятный, заранее объяснимый размен. Bond-лосс -- единственное чётко \emph{отрицательное} проверенное добавление (в сумме ${-}0.015$, основное ухудшение в полноте пептидов), поэтому в основную модель он не входит и служит примером того, что не всякая правдоподобная идея окупается.

\paragraph{Про честность оценки.} Помимо самих результатов, в работе исправлена ошибка в подсчёте $\pm3$-метрики (занижавшая полноту) и построен аккуратный протокол с явными ролями фолдов, отделяющий выбор архитектуры от итоговой оценки. Все сравнения снабжены доверительными интервалами по белкам. Единого «чемпиона» поверх перечисленного нет: на фолдах разного состава лидер меняется, поэтому корректнее описывать области доминирования основных моделей.

\section{Ограничения и дальнейшая работа}
\begin{itemize}
  \item Нет полной перекрёстной проверки -- это единственный способ получить одно чистое глобальное число с честным выбором архитектуры, отложено из-за вычислительной сложности.
  \item Не осталось отдельного тестового фолда, который не использовался бы при выборе архитектуры (следствие объединения фолдов).
  \item Доверительные интервалы учитывают только разброс по белкам (не по сидам и не по обучению), поэтому оценка консервативна.
  \item Не проведён честный кросс-датасетный тест (обучение на данных 2022 года, проверка на новых семействах 2026 года). Обобщение на новые последовательности оценивается пока только косвенно -- через гомологичное разбиение внутри одного датасета.
  \item Многие эксперименты не проверены на чистом протоколе, а некоторые даже на старом, например: Ankh, дообучение pLM и контрастное обучение, трансформер-голова поверх эмбеддингов.
\end{itemize}

\bibliographystyle{plain}
\bibliography{refs}

\end{document}
